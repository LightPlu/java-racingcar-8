# 구현 전 생각해낸 예외상황
- 만약 자동차가 아무도 전진을 하지 않는다면 어떻게 처리할 것인가?
  - 최종 우승자 출력 단계에서 따로 예외 메시지를 만들어 출력한다("아무 자동차도 전진하지 못하여 우승자는 없습니다")
- 만약 이름 중간에 공백이 온다면 정상 입력 처리 받을 것인가?
  - 사람마다 이름은 다르므로 이름 사이의 공백은 옳은 입력으로 처리한다. 하지만 이름 시작과 끝지점의 공백은 잘못된 입력으로 처리한다
- 만약 사용자가 시도할 횟수를 0회로 입력한다면 어떻게 처리할 것인가?
  - 시도할 횟수를 0회로 작성한다면 자동차 경주를 시도조차 하지 못하므로 이 경우는 잘못된 입력으로 처리한다
- 만약 사용자가 시도할 횟수를 소숫점으로 입력한다면 어떻게 처리할 것인가?
  - 횟수에 대해서는 소숫점 개념이 없으므로 이 또한 잘못된 입력으로 처리한다
- 만약 사용자가 시도할 횟수를 음수로 입력한다면 어떻게 처리할 것인가?
  - 횟수에 대해서는 음수의 개념이 없으므로 이 또한 잘못된 입력으로 처리한다
